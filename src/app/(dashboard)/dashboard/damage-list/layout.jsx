export const metadata = {
  title: 'Damage Log & History | Admin Dashboard',
  description: 'View damage logs and withdrawn stock records.'
}

export default function DamageListLayout({ children }) {
  return <>{children}</>
}
