export const metadata = {
  title: 'Customer Support | Dashboard',
  description: 'Manage customer support messages, inquiries, and email replies.'
}

export default function SupportLayout({ children }) {
  return <>{children}</>
}
