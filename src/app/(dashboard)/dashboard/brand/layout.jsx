export const metadata = {
  title: 'Brand Management | Dashboard',
  description: 'Manage product brands, statuses, and details.'
}

export default function BrandLayout({ children }) {
  return <>{children}</>
}
