export const metadata = {
  title: 'My Profile | Dashboard',
  description: 'Manage staff credentials, security, and account preferences.'
}

export default function AccountLayout({ children }) {
  return <>{children}</>
}
