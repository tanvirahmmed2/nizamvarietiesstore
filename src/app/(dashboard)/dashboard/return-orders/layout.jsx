export const metadata = {
  title: 'Return Orders | Dashboard',
  description: 'Manage order returns, refunds, and customer exchanges.'
}

export default function ReturnOrdersLayout({ children }) {
  return <>{children}</>
}
