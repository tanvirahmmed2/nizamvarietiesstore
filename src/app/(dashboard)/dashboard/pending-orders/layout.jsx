export const metadata = {
  title: 'Pending Orders | Dashboard',
  description: 'Manage pending customer orders, approvals, and dispatch.'
}

export default function PendingOrdersLayout({ children }) {
  return <>{children}</>
}
