export const metadata = {
  title: 'Product Categories | Nizam Store',
  description: 'Browse products by category and explore available inventory.'
}

export default function CategoryProductsLayout({ children }) {
  return <>{children}</>
}
