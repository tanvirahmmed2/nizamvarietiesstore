export const metadata = {
  title: 'My Orders | Nizam Store',
  description: 'View order history, status updates, and purchase tracking.'
}

export default function UserOrdersLayout({ children }) {
  return <>{children}</>
}
